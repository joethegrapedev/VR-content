using UnityEngine;

/// <summary>
/// Marks a button as already bound by <see cref="UiSoundBinder"/> so repeated
/// rebinds do not stack duplicate listeners on the same control.
/// </summary>
public class UiSoundMarker : MonoBehaviour
{
}
